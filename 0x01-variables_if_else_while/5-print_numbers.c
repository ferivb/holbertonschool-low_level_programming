#include <stdio.h>
#include <stdlib.h>

/**
  * main - Entry point
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
	char n = 48;

	for (n = 48; n <= 57; n++)
	{
	putchar(n);
	}
	putchar(10);
	return (0);
}
